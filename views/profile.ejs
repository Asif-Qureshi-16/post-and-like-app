<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Account</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
  </head>
  <body class="">
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="nav mb-10 text-blue-500 flex justify-around">
        <a
          href="/"
          class="bg-blue-500 rounded-md px-3 py-2 text-sm mb-5 inline-block text-white"
          >Back to Register Page</a
        >
        <a
          href="/logout"
          class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block text-white"
          >Logout</a
        >
      </div>
      <h1 class="text-3xl tracking-tighter mb-3">Welcome, <%=user.name %></h1>
      <h5>You Can Create a New Post</h5>
      <form action="/post" method="post">
        <textarea
          name="content"
          placeholder="Whats in your mind"
          class="m-2 border-none w-1/2 h-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
        >
        </textarea>
        <input
          type="submit"
          class="w-80 h-10 px-3 py-2 bg-blue-500 rounded-md mt-2 block"
          value="Create Post"
        />
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your POST</h3>
        <div class="post-container mt-5 flex gap-3">
          <% user.posts.reverse().forEach(post => { %>
          <div class="post w-1/3 p-4 border-2 rounded-md border-zinc-500">
            <h4 class="mb-3 text-blue-500"><%=user.email%></h4>
            <p class="text-sm tracking-tight"><%=post.content %></p>
            <small><%=post.likes.length %></small>
            <div class="btns flex gap-4 mt-5">
              <a href="/like/<%= post._id %>" class="text-blue-500">
                <% if (post.likes.indexOf(user._id) === -1) { %>
                <i class="fa-regular fa-heart"></i>
                <% } else { %>
                <i class="fa-solid fa-heart"></i>
                <% } %>
              </a>
              <a href="/edit/<%=post._id  %>" class="text-zinc-500">Edit</a>
            </div>
          </div>
          <% }); %>
        </div>
      </div>
    </div>
  </body>
</html>
